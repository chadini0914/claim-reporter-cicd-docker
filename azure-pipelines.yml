

trigger:
- master

variables:
  vmImageName: 'ubuntu-latest'

stages:

- stage: Build
  displayName: Build_stage

  jobs:
    - job: docker
      displayName: docker_job
      pool:
        vmImage: $(vmImageName)






